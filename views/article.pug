extends layout

block content
    h1= article.title
    h5 Written by #{article.author}
    p= article.body
    hr
    a(href='/articles/edit/'+article._id)
     input.btn.btn-primary(type="button", value='Edit')
    br
    a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete